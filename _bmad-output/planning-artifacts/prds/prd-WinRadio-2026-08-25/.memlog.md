---
topic: WinRadio PRD
updated: 2026-08-25T18:38
---

- (decision) User selected Fast path for PRD discovery.
- (decision) User provided a reference screenshot (Windows 8-era 'Internet Radio' Metro app) as the desired look-and-feel AND functional target -- confirmed 'I need something like this image' after being asked whether the map/weather/IP tiles are real features vs just aesthetic.
- (change) SCOPE DELTA vs finalized brief.md: adds three new v1 features not in the original brief -- station geolocation map tile, local weather tile for the station's city, and a stream/IP technical-info tile. Reason: explicit user request driven by the reference image. Original brief.md is NOT being retroactively edited; this is logged as a conscious expansion at PRD stage.
- (assumption) Weather tile backed by Open-Meteo (free, no API key) -- user had no preference; chosen to match relaxed/no-ops-burden project ethos.
- (assumption) Map tile is a simple embedded/static map (OpenStreetMap-based, no paid API key) showing the station's city/coords, not a full interactive map widget -- lower complexity fit for a personal project.
- (assumption) IP/stream-info tile sources bitrate/codec/country from Radio-Browser station metadata and resolves the stream host to an IP via DNS for display -- matches the reference image's info tile.
- (decision) User Journeys section downscaled to a single lightweight session walkthrough per PRD Discipline guidance for hobby/solo single-operator products, rather than a formal multi-UJ section.
- (assumption) New optional tiles (weather, map, IP info) are NFR'd as non-blocking enhancements: if their external service is unreachable, core playback must still work -- prevents new external dependencies from reintroducing fragility.
- (change) Input reconciliation against brief.md caught an unlogged scope drift: FR-13 had silently pulled 'start-minimized' into v1 settings while brief.md deferred it bundled with auto-start-with-Windows. Fixed: removed start-minimized from FR-13, added it explicitly to the SS6.2 deferred list alongside auto-start-with-Windows.
- (change) Reconciliation also folded back: brief.md's concrete usability bar (search/play/favorite in a few clicks) into SM-1, the specific broken-wiring evidence (unwired commands module, router-less router-view) into Open Question 1, and the 'no onboarding flow' constraint into SS2 Target User.
- (event) Rubric reviewer pass: overall strong, 0 critical/high findings. Fixed the one medium finding (NFR-2 bounds turned into concrete numbers: 3s search, 2s playback start, ~150MB idle RSS target) and the low finding (added per-FR failure-state consequences to FR-7/8/9). Also closed mechanical nits: SS2 numbering gap (2.3->2.2), 'Sleep timer' capitalization in FR-11 heading, and added a conscious note on why FR-12-14 have no dedicated Success Metric.
- (event) Open-item triage: none of SS10's 3 Open Questions are phase-blockers -- all three are deliberately architecture-phase inputs (repair-vs-restart, codec support, existing-component reuse), owner=architecture phase, revisit condition=when bmad-architecture runs. The SS6.2 [NOTE FOR PM] on English-only UI is a non-blocker, owner=Javad, revisit condition=if WinRadio stops being personal-use-only. All 8 SS11 assumptions were already reviewed by the user in the draft pass and accepted ('good') -- none block finalization.
- (event) Polish pass (structure+prose): checked all section cross-references (grep for section-symbol patterns) and found one broken ref -- Document Purpose said assumptions were 'indexed in section 9' but the Assumptions Index is actually section 11 (section 9 is Aesthetic and Tone). Fixed. No other structural redundancy or prose issues found; content substance already validated strong by the rubric reviewer pass.
- (event) PRD finalized

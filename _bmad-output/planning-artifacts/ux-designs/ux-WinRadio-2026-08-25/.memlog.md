---
topic: WinRadio UX
updated: 2026-08-25T19:06
---

- (decision) User selected Fast path for UX discovery (overriding my Coaching-path suggestion) -- skip creative tools, draft both spines directly with [ASSUMPTION] tags.
- (event) User re-shared the Windows-8-era 'Internet Radio' Metro app screenshot as the visual/functional reference. No binary file was recoverable from the chat attachment to place in imports/ -- treating the detailed description already captured (dark tile dashboard, left station-list rail with live count, large now-playing panel with disc/artwork + transport bar, three small Info Tiles: artwork/logo, location map, and a text info tile; weather panel below; top nav of circular icon buttons for Home/Favorites/Filter/Search) as the de facto import, sourced from this conversation rather than a file.
- (decision) IA closure: single-window dashboard app, not multi-page navigation -- Station List rail (favorites by default, swaps to search results while searching) + Now-Playing Dashboard (persistent) + Settings as a modal overlay (matches existing SettingsModal.vue already in the codebase) + Tray as a background/minimized system state. No separate screens invented beyond what PRD's FRs require.
- (assumption) Color palette [ASSUMPTION] inferred from the reference image: near-black background (~#101012), off-white primary text, a violet/purple accent (~#7C5CFF) for active/interactive elements (volume fill, focus rings), a cooler blue for station-list link text (~#5AA9F0). No hex values were given by the user -- these are a starting point to correct.
- (assumption) Typography [ASSUMPTION]: native Windows UI font stack (Segoe UI Variable / Segoe UI fallback), matching the reference image's system-font look and fitting a Tauri desktop app with no custom webfont loading needed.
- (assumption) Shape language [ASSUMPTION]: flat/minimal rounding on tiles and cards (Metro-style, matches reference), fully circular nav icon-buttons (also matches reference) -- a deliberate two-track shape system rather than one border-radius value everywhere.
- (decision) Accessibility floor: even though this is a single personal user, baking in keyboard operability (play/pause/volume/search all keyboard-reachable) and visible focus states from the start, since it costs little now and PRD NFR-3/NFR-4 already commit to graceful-degradation discipline that pairs naturally with it.
- (change) Proactive Pass-1 coverage self-check (component + token completeness) caught: EXPERIENCE.md's Component Patterns table referenced a 'Search field' component with no matching DESIGN.md visual spec or frontmatter token. Fixed: added components.search-input token + DESIGN.md.Components bullet.
- (change) Coverage check also flagged Accessibility Floor's contrast claim as vague ('AA' with no numbers). Fixed: stated estimated contrast ratios per pairing, flagged on-surface-variant as the one pairing worth re-verifying against rendered mocks, tagged [ASSUMPTION] since ratios are hex-estimated not measured.
- (change) Input reconciliation against prd.md and brief.md found 10+3 gaps. Fixed the substantive ones: added Sleep Timer UI (icon control + active badge + duration picker, no persistent countdown -- understated per brand posture), fixed Stream Info Tile's missing resolved-IP field and its conflation with Location Tile's coordinates in DESIGN.md typography, added live track/title typography treatment, added explicit mute toggle distinct from volume-zero, added Favorites reorder as a real component spec (always-visible up/down controls, not just a 'banned drag' aside), specified the Filter control as a chip-based multi-select panel, placed the theme toggle inside the Settings modal (new settings-modal component), and differentiated 'offline' from 'genuine zero search results' with distinct copy.
- (change) Also fixed: removed a phantom 'Home' nav icon -- traced back to the reference image's GER/ENG language-toggle links (already correctly excluded) being mislabeled as a generic nav icon in the first draft. Softened Foundation/IA language that implied the existing SettingsModal.vue/Tailwind scaffold would definitely be reused, since repair-vs-rebuild is still an open PRD/architecture question. Added brief.md's explicit non-goals (no EQ, no casting, no recording, no accounts) to the Anti-patterns list. Established 'the rail' as an explicit sanctioned shorthand for the Glossary term 'Station List' in EXPERIENCE.md.Foundation, and added one Station List reference into DESIGN.md (which previously never used the Glossary term at all).
- (event) Open-item triage: 5 tags total ([ASSUMPTION] x3: color hex values, contrast ratios, typography font stack; [NOTE FOR UX] x2: Home-icon removal rationale, deliberate accessibility-rigor-beyond-brief-tone). None are phase-blockers. Color hex + contrast ratios: revisit when key-screen mocks render (next step) and again if this hands off to an external design tool. Scaffold repair-vs-rebuild note: owner=architecture phase, mirrors PRD Open Question 1, not this workflow's to resolve. User skipped the optional Reviewer Gate as recommended given hobby stakes and the coverage-check + reconciliation passes already run.
- (event) Rendered 3 key-screen HTML mocks (Main Dashboard, Search state, Settings modal) covering all visual IA surfaces -- Tray is correctly the only spine-only surface, since it has no visual form. Verified all three render correctly in-browser. Promoted from .working/ to mockups/, linked inline in EXPERIENCE.md's IA table and Flow 1.
- (change) Polish-pass self-check caught a second missing-component gap of the same shape as the earlier Search-field one: EXPERIENCE.md's Filter control row (and the search-state.html mock's filter chip) had no DESIGN.md visual spec. Fixed: added components.filter-chip token + DESIGN.md.Components bullet.
- (event) spines finalized

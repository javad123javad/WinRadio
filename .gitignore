# Rust
/winradio/src-tauri/target/

# Node
/winradio/node_modules/
/winradio/dist/

# Editor / OS
.vscode/
.idea/
*.swp
Thumbs.db
.DS_Store

# BMad Method tooling (generic, reusable skill/config framework — not
# WinRadio-specific). _bmad-output/ (specs, retros, deferred-work.md) is
# kept: that's the project's own decision history, not the tooling itself.
/_bmad/
/.claude/skills/
